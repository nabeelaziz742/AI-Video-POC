import { CharacterDraft } from "../character-types";

export function CharacterEditor({ characters, setCharacters }: { characters: CharacterDraft[]; setCharacters: (value: CharacterDraft[]) => void }) {
  const update = (index: number, key: keyof CharacterDraft, value: string) => setCharacters(characters.map((item, i) => i === index ? { ...item, [key]: value } : item));
  return <div className="space-y-5">{characters.map((character, index) => <div key={index} className="rounded-xl border border-white/10 bg-black/20 p-4"><div className="mb-3 flex items-center justify-between"><span className="text-xs font-medium text-violet-300">Character {index + 1}</span>{characters.length > 1 && <button type="button" onClick={() => setCharacters(characters.filter((_, i) => i !== index))} className="text-xs text-red-300/60">Remove</button>}</div><div className="grid gap-3 sm:grid-cols-2">{([['name','Name'],['role','Role'],['age_description','Age'],['appearance','Appearance'],['clothing','Clothing'],['personality','Personality']] as const).map(([key,label]) => <input key={key} value={character[key]} onChange={(e) => update(index,key,e.target.value)} placeholder={label} aria-label={label} className="rounded-lg border border-white/10 bg-white/[0.03] px-3 py-2.5 text-xs outline-none placeholder:text-white/20 focus:border-violet-500/50" />)}</div><textarea value={character.description} onChange={(e) => update(index,"description",e.target.value)} placeholder="Character description" rows={2} className="mt-3 w-full rounded-lg border border-white/10 bg-white/[0.03] px-3 py-2.5 text-xs outline-none" /><input value={character.visual_prompt} onChange={(e) => update(index,"visual_prompt",e.target.value)} placeholder="Visual style" className="mt-3 w-full rounded-lg border border-white/10 bg-white/[0.03] px-3 py-2.5 text-xs outline-none" /></div>)}</div>;
}
